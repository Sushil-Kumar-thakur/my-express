<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Registration Successful</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #f0fdf4;
      color: #333;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .box {
      text-align: center;
      background: #ffffff;
      padding: 2rem 3rem;
      border-radius: 16px;
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
    }
    h1 {
      font-size: 1.8rem;
      color: #16a34a;
      margin-bottom: 0.5rem;
    }
    p {
      font-size: 1rem;
      margin-top: 0;
    }
    a {
      text-decoration: none;
      color: #2563eb;
      font-weight: 500;
      display: inline-block;
      margin-top: 1.5rem;
    }
   .user_name {
  font-size: 1.2rem;
  font-weight: 600;
  color: #16a34a;
  text-overflow: ellipsis;
  animation: glowBlink 1.5s ease-in-out infinite alternate;
    }

    /* Keyframes for glowing + blinking */
    @keyframes glowBlink {
    0% {
        color: #4fc57a;
        text-shadow: 0 0 4px #2daa5b;
    }
    50% {
        color: #5ea155;
        text-shadow: 0 0 8px #1e793f, 0 0 10px #2e8b50;
    }
    100% {
        color: transparent;
    text-shadow: 0 0 2px #2daa5b;
    }
    }

  </style>
</head>
<body>
  <div class="box">
    <h1>✅ Registration Successful! </h1>
    <p >Welcome, <span class="user_name"><%= name %></span> ! Your data has been saved.</p>
    <a href="/user-details">  📋 Registration page </a> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
     <a href="/">  🏠 Go to Home </a>
     
  </div>
</body>
</html>
